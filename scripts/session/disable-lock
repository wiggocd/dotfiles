#!/usr/bin/env bash

LOCKER=xss-lock
[ -n "${LOCK_DISABLED}" ] || export LOCK_DISABLED="/tmp/nolock-${UID}.lock"

xset -dpms
xset s off
! ps -C $LOCKER >/dev/null 2>&1 || killall $LOCKER

touch "$LOCK_DISABLED"
