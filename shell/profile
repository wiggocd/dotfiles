source /etc/profile

export SCRIPTS="$HOME/.local/scripts"
export PATH="$HOME/.local/bin:$SCRIPTS:$PATH"

alias ll="ls -l"
alias la="ls -al"
alias lsd="ls -ld"
alias c="clear"
alias pac="sudo pacman"
alias aur="pikaur"
alias setrandr="touch $RANDRLOCK >/dev/null; \
                autorandr -l"